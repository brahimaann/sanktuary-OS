# Copy to .env in this folder. Never commit .env.
# Password for Nextcloud's database account; any long random string.
DB_PASSWORD=change-me-to-a-long-random-string
# Managed by drive-watch.ps1 (the Seagate's current letter, or ./offline while it is unplugged)
DATA_SRC=H:/
DATA_DST=/mnt/h
